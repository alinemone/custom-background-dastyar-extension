# 🎨 ابزار شخصی‌سازی بک‌گراند برای اکستنشن Dastyar
# Custom Background Tool for Dastyar Extension

## 📋 فهرست محتوا / Table of Contents
1. [معرفی](#معرفی)
2. [نصب و راه‌اندازی](#نصب-و-راهاندازی)
3. [نحوه استفاده](#نحوه-استفاده)
4. [آپدیت اکستنشن](#آپدیت-اکستنشن)
5. [سفارشی‌سازی پیشرفته](#سفارشیسازی-پیشرفته)

---

## 📝 معرفی

این ابزار به شما امکان می‌دهد بدون تغییر کد اصلی اکستنشن Dastyar، بک‌گراند دلخواه خود را تنظیم کنید.

### ویژگی‌ها:
✅ بدون نیاز به تغییر کد اصلی اکستنشن
✅ پشتیبانی از گرادینت‌های مختلف
✅ امکان استفاده از رنگ دلخواه
✅ امکان استفاده از تصویر سفارشی
✅ رابط کاربری گرافیکی ساده
✅ ذخیره خودکار تنظیمات
✅ آسان برای اعمال مجدد پس از آپدیت اکستنشن

---

## 🚀 نصب و راه‌اندازی

### مرحله 1: بررسی ساختار فایل‌ها

پس از اجرای این ابزار، یک فولدر به نام `custom-background` در مسیر اکستنشن ایجاد شده که شامل فایل‌های زیر است:

```
4.2.4_0/
├── custom-background/          ← فولدر جدید
│   ├── custom-bg.css          ← استایل‌های شخصی‌سازی
│   ├── custom-bg.js           ← اسکریپت مدیریت بک‌گراند
│   ├── README.md              ← این فایل راهنما
│   └── INTEGRATION.md         ← دستورالعمل یکپارچه‌سازی
├── manifest.json              ← نیاز به ویرایش جزئی دارد
└── dist/
    └── override/
        └── index.html         ← نیاز به ویرایش جزئی دارد
```

---

### مرحله 2: ویرایش فایل `manifest.json`

فایل `manifest.json` را باز کنید و تغییرات زیر را اعمال کنید:

#### قبل از تغییر:
```json
{
  "chrome_url_overrides": {
    "newtab": "./dist/override/index.html"
  },
  "web_accessible_resources": [ {
    "matches": [ "<all_urls>" ],
    "resources": [ "assets/img/lib/*" ]
  } ]
}
```

#### بعد از تغییر:
```json
{
  "chrome_url_overrides": {
    "newtab": "./dist/override/index.html"
  },
  "web_accessible_resources": [ {
    "matches": [ "<all_urls>" ],
    "resources": [
      "assets/img/lib/*",
      "custom-background/*"
    ]
  } ]
}
```

**نکته مهم:** فقط `"custom-background/*"` را به آرایه `resources` اضافه کنید.

---

### مرحله 3: ویرایش فایل `dist/override/index.html`

فایل `dist/override/index.html` را باز کنید و دو خط زیر را **قبل از تگ `</head>`** اضافه کنید:

```html
<!doctype html>
<html lang="fa" dir="rtl">
	<head>
		<meta charset="UTF-8" />
		<base target="_blank" />
		<title>New tab</title>
		<script type="module" crossorigin src="/dist/assets/override-DDlCyY7i.js"></script>
		<link rel="modulepreload" crossorigin href="/dist/assets/_commonjsHelpers-CNBic1d4.js">
		<link rel="modulepreload" crossorigin href="/dist/assets/vendor-BxtyZXV1.js">
		<link rel="modulepreload" crossorigin href="/dist/assets/ui-rnakXG1H.js">
		<link rel="stylesheet" crossorigin href="/dist/assets/override-DIrjmJxV.css">

		<!-- 🎨 Custom Background Tool - شروع -->
		<link rel="stylesheet" href="/custom-background/custom-bg.css">
		<script src="/custom-background/custom-bg.js"></script>
		<!-- 🎨 Custom Background Tool - پایان -->
	</head>

	<body class="override-body">
		<div id="app"></div>
	</body>
</html>
```

---

### مرحله 4: بارگذاری مجدد اکستنشن

1. به آدرس `chrome://extensions/` بروید
2. حالت Developer را فعال کنید
3. روی دکمه "Reload" اکستنشن Dastyar کلیک کنید
4. یک تب جدید باز کنید

---

## 🎯 نحوه استفاده

پس از نصب موفق، یک دکمه 🎨 در گوشه پایین سمت چپ صفحه نمایش داده می‌شود.

### استفاده از رابط گرافیکی:

1. روی دکمه 🎨 کلیک کنید
2. نوع بک‌گراند مورد نظر را انتخاب کنید:
   - **گرادینت:** از گرادینت‌های آماده استفاده کنید
   - **رنگ ساده:** یک رنگ دلخواه انتخاب کنید
   - **تصویر:** آدرس URL یک تصویر را وارد کنید
   - **پیش‌فرض:** بازگشت به حالت اولیه
3. افکت‌های اضافی (اختیاری):
   - ✅ لایه تیره: برای خوانایی بهتر متن
   - ✅ افکت blur: برای ظاهر مدرن‌تر
4. روی "اعمال" کلیک کنید

تنظیمات شما به صورت خودکار ذخیره می‌شود.

---

## 🔄 آپدیت اکستنشن

وقتی اکستنشن Dastyar به نسخه جدید آپدیت می‌شود:

### مراحل:

1. **کپی کردن فولدر:**
   - فولدر `custom-background` را از نسخه قبلی کپی کنید
   - در نسخه جدید جایگذاری کنید

2. **اعمال تغییرات:**
   - مرحله 2 (ویرایش `manifest.json`)
   - مرحله 3 (ویرایش `index.html`)
   - را دوباره انجام دهید

3. **بارگذاری مجدد:**
   - اکستنشن را Reload کنید
   - تمام تنظیمات شما حفظ می‌شود

**نکته:** چون همه کدهای شخصی‌سازی در فولدر `custom-background` هستند، تنها کاری که باید انجام دهید کپی این فولدر و اعمال 2 تغییر جزئی است.

---

## 🎨 سفارشی‌سازی پیشرفته

### اضافه کردن گرادینت سفارشی:

فایل `custom-background/custom-bg.css` را ویرایش کنید:

```css
/* گرادینت سفارشی من */
body.bg-my-custom-gradient {
    background: linear-gradient(135deg, #FF6B6B 0%, #4ECDC4 100%) !important;
}
```

سپس در فایل `custom-background/custom-bg.js`، در قسمت تعریف `gradient-select`، گزینه جدید را اضافه کنید:

```html
<option value="bg-my-custom-gradient">گرادینت من</option>
```

### استفاده از تصویر محلی:

1. تصویر خود را در فولدر `custom-background` قرار دهید
2. در پنل تنظیمات، آدرس را به این صورت وارد کنید:
   ```
   /custom-background/my-image.jpg
   ```

### تغییر موقعیت دکمه تنظیمات:

در فایل `custom-bg.js`، خط مربوط به `button.style.cssText` را ویرایش کنید:

```javascript
// برای تغییر به گوشه راست پایین:
bottom: 20px;
right: 20px;  // به جای left: 20px;
```

---

## 🛠️ عیب‌یابی

### بک‌گراند اعمال نمی‌شود:
- مطمئن شوید فایل‌های `custom-bg.css` و `custom-bg.js` به درستی در `index.html` لینک شده‌اند
- کنسول مرورگر را باز کنید (F12) و خطاها را بررسی کنید
- اکستنشن را Reload کنید

### دکمه 🎨 نمایش داده نمی‌شود:
- مطمئن شوید `custom-bg.js` به درستی لود شده است
- کش مرورگر را پاک کنید (Ctrl+Shift+Delete)

### تصویر بک‌گراند نمایش داده نمی‌شود:
- مطمئن شوید URL تصویر معتبر است
- از تصاویر با فرمت JPG, PNG, WebP استفاده کنید
- اگر از تصویر محلی استفاده می‌کنید، مطمئن شوید در فولدر `custom-background` قرار دارد

---

## 📞 پشتیبانی

این ابزار به صورت مستقل از اکستنشن Dastyar توسعه داده شده است.
- برای مشکلات مربوط به این ابزار، فایل‌های موجود در `custom-background` را بررسی کنید
- برای مشکلات اکستنشن اصلی، به سازنده اکستنشن Dastyar مراجعه کنید

---

## 📄 لایسنس

این ابزار به صورت رایگان و متن‌باز در اختیار شما قرار گرفته است.

---

**ساخته شده با ❤️ برای کاربران Dastyar**
